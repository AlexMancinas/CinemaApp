import { ValidateTicketBuyPipe } from './validate-ticket-buy.pipe';

describe('ValidateTicketBuyPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateTicketBuyPipe();
    expect(pipe).toBeTruthy();
  });
});
